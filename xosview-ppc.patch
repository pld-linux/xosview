--- xosview-1.7.3/linux/MeterMaker.cc	Fri Nov 12 00:13:22 1999
+++ xosview-1.7.3/linux/MeterMaker.cc.ppc	Thu Jan 25 07:48:31 2001
@@ -56,7 +56,7 @@
       push(new DiskMeter(_xos, atof(_xos->getResource("diskBandwidth"))));
 
   // check for the serial meters.
-#ifndef __mc68000__
+#if !defined(__mc68000__) && !defined(__powerpc__)
   for (int i = 0 ; i < SerialMeter::numDevices() ; i++)
     if (_xos->isResourceTrue(SerialMeter::getResourceName(
       (SerialMeter::Device)i)))

--- xosview-1.7.3/config/Makefile.linux.in	Fri Nov 12 00:13:22 1999
+++ xosview-1.7.3/config/Makefile.linux.in.ppc	Thu Jan 25 07:48:19 2001
@@ -22,11 +22,22 @@
 raidmeter.o \
 MeterMaker.o
 
+addserial = 
+
 ifeq (@host_cpu@, m68k)
-# nothing else for m68k
+  addserial = no
+endif
+
+ifeq (@host_cpu@, powerpc)
+  addserial = yes
+endif	
+
+ifdef addserial
+# nothing else for m68k, powerpc
 else
 OBJS += serialmeter.o
 endif
+
 
 CFILES := $(OBJS:.o=.cc)
 DEPFILES := $(OBJS:%=.%.d)
